<div class="mx-auto max-w-md">
  <h1 class="text-2xl font-bold"><%= t("product_variants.new.title", product: @product.name) %></h1>

  <%= render "form", product: @product, product_variant: @product_variant %>
</div>
