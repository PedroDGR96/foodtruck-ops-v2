<%= content_for :title, t("customers.edit.title") %>

<div class="mx-auto max-w-md">
  <h1 class="text-2xl font-bold"><%= t("customers.edit.title") %></h1>
  <%= render "customers/form", customer: @customer %>
</div>
